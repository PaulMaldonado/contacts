<%- include("../partials/head.ejs") %>
<%- include("../partials/navigation.ejs") %>

  <div class="container">
    <div class="row mt-4">
      <div class="col-md-8 col-sm-12 col-lg-8 col-xl-8 mx-auto">
        <div class="card">
          <div class="card-header">
            Editar Contacto
          </div>
          <div class="card-body">
            <form action="/edit/<%= result.id %>" method="POST">
              <div class="form-group">
                <input type="text" value="<%= result.name %>" name="name" placeholder="Editar nombre" class="form-control">
              </div>
              <div class="form-group">
                <input type="email" value="<%= result.email %> " name="email" placeholder="Editar correo electronico" class="form-control">
              </div>
              <div class="form-group">
                <input type="number" value="<%= result.phone %> " name="phone" placeholder="Editar número de telefono" class="form-control">
              </div>

              <input type="submit" value="Editar" class="btn btn-primary btn-block">
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

<%- include("../partials/footer.ejs") %> 